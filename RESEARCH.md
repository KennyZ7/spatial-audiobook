# 树莓派空间渲染研究准备

当前是工程原型与传统DSP基线，尚无新算法验证结论，也没有树莓派性能结果。发表目标为CCF-C或以上，投稿方向和创新点需要文献与实验证据确定。

## 可复现实验入口

`python scripts/benchmark.py --seconds 3 --out data/benchmark.json` 对 H3/H4/H5 与1、4、8个运动声源输出 wall time、CPU time、实时系数和峰值。保存了随机种子、平台、Python版本、渲染版本。该脚本不联网，不调用配音服务。

工程分轨包保存原文、改编、角色、事件、轨迹和实际编排时间。`data/hrtf/*.json` 保存数据源与 SHA256。后续模型通过兼容 `HRIR.at(position)` 的接口返回左右滤波器，或实现接受同一音源列表的渲染器，前端与语音模块无需修改。

## 树莓派阶段

在 Raspberry Pi 5 8GB、64位系统上创建Python环境，安装依赖并拷贝工程分轨与HRTF。先测离线基线，再实现固定块长的音频设备回调；记录不同声源数下的CPU、RSS峰值、欠载次数、温度、限频状态。USB声卡或耳机接口、散热方式与电源型号作为实验条件一并记录。

服务器仅提供干声与空间描述；树莓派接入真实音频设备的流式播放尚未实现。离线处理实时系数小于1不能证明端到端低延迟。

## 候选研究假设

评估少量滤波基底加小网络预测方向权重，配合显式耳间时延，能否在树莓派计算预算下保留空间线索。先与相同基底的非学习插值、直接卷积和优化分块卷积比较，排除仅靠工程优化就能取得的收益。

按听众对象划分训练、验证、测试；禁止将同一听众相邻方向随机分到训练和测试。3套首版配置只供界面试听，不足以直接支撑模型训练结论；后续下载更多对象并核对使用许可。

指标包括谱形失真、ITD/ILD误差、方向辨认误差、前后混淆、主观音质与设备资源占用。开展多个真实听众的盲听，报告人数、设备、顺序随机化与统计不确定性。界面自动化生成的“听感记录”不进入论文数据。

训练预算、研究周期与会议选择在基线实验和文献调研后单独制定；本次100元预算不用于云GPU训练。
