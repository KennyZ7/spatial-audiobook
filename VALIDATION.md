# 首版验证记录

## 2026年9月10日补充验证

最新测试：37 passed，2条第三方弃用提示。新增验证覆盖 during 规范化、未知长度流式 FLAC 的有界解码、失败后原始音频缓存重用，以及历史成品 HTML/CSV 导出。报告浏览器检查确认44个事件、筛选、搜索和轨迹展开。实际服务调用与配音生成已在本机使用阶段开展；原始密钥、调用账本和缓存不进入仓库。主观定位与树莓派验证仍待开展。

以下保留2026年9月8日首版记录，其中测试数量与实际接入状态描述只适用于当时版本。

验证环境：Windows 11、Python 3.12.14，渲染器 `hrir-ola-1.1`。日期：2026-09-08。

## 已通过

- `python -m pytest -q`：16 passed。存在2条来自第三方测试客户端的弃用提示，无测试失败。
- 覆盖500字和2000字、忠实和改编两种模式的脚本解析、语音缓存、双声道渲染、分轨ZIP导出与免费重渲染。在线模型响应使用模拟数据，未冒充真实API调用。
- 原文跨度无遗漏；忠实模式不采纳模型改写；原文改编关系保留；不合法或缺段脚本不缓存。
- 汉字费用计数、预算预留、超额阻断、无效密钥/超时提示、错误消息不回显密钥。
- 句前、句后、句内及绝对时间编排，素材缺失阻断，最大8个点声源并发约束。
- 真实 SADIE H3/H4/H5 数据的左右耳能量方向正确；远近衰减正确；上下方向滤波器存在差异。这些数值检查不等于主观定位验证。
- 重叠相加常量增益、有限数值、48kHz双声道与峰值检查；失败保留已有输出，重启恢复中断任务。
- 浏览器桌面1440px和移动390px检查：无页面脚本错误，无横向溢出；已检查截图。
- 浏览器点击免费重渲染后，费用仍为0元；ZIP下载成功，文件签名为PK；WAV Range请求返回206与RIFF头。
- 自动化脚本提交的试听记录明确写为“自动化界面验证，非用户听感评价”，不能用于听测数据。

## 演示和性能

已生成约142.07秒的悬疑示例，使用Windows系统配音及本地素材。一次浏览器触发的H3离线渲染耗时11.49秒，实时系数约0.081；结果受当前电脑负载影响，不是树莓派数据。

`data/benchmark.json` 保存三套配置×1/4/8个运动声源的9组基线。该基准不测设备输出延迟、热限制、持续流式播放欠载或听感。

## 尚待实际验收

1. 用户提供API Key并核对费率后，验证真实DeepSeek脚本与MiniMax多角色情感配音、小额实际账单。
2. 用户戴耳机评价前后、上下、距离、运动和配音听感，选择HRTF配置。当前没有真实用户听测结论。
3. 树莓派部署、小模型训练、多听众实验与论文研究属于后续阶段，尚未实施。
